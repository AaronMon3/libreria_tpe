<?php 
require_once "app/views/templates/layout/header.phtml";
?>

<main class="admin">
  <h2>Administración de Géneros</h2>
  <p><a href="admin/generos/agregar">Agregar nuevo género</a></p>

  <table class="admin-table">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Descripción</th>
        <th>Imagen</th>
        <th>Acciones</th>
      </tr>
    </thead>
    <tbody>
      <?php foreach($genres as $genre) { ?>
        <tr>
          <td><a href="generos/<?= $genre->id_genero ?>"><?= htmlspecialchars($genre->nombre) ?></a></td>
          <td><?= htmlspecialchars($genre->descripcion) ?></td>
          <td>
            <?php if (!empty($genre->imagen)) { ?>
              <img src="<?= htmlspecialchars($genre->imagen) ?>" alt="Imagen de <?= htmlspecialchars($genre->nombre) ?>" class="admin-table__img">
            <?php } else { ?>
              <span class="admin-table__no-img">Sin imagen</span>
            <?php } ?>
          </td>
          <td>
            <a href="admin/generos/editar/<?= $genre->id_genero ?>">Editar</a>
            <a href="admin/generos/eliminar/<?= $genre->id_genero ?>"
               onclick="return confirm('¿Eliminar este género?');">Eliminar</a>
          </td>
        </tr>
      <?php } ?>
    </tbody>
  </table>
</main>

<?php
require_once "app/views/templates/layout/footer.phtml";
?>
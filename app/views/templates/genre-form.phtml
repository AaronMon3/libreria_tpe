<?php
require_once "app/views/templates/layout/header.phtml";
$isEdit = $genre !== null;
$action = $isEdit ? "admin/generos/editar/" . $genre->id_genero : "admin/generos/agregar";
?>

<main class="form-page">
  <h2><?= $isEdit ? "Editar género" : "Agregar género" ?></h2>

  <form method="POST" action="<?= $action ?>" class="genre-form">
    <label>
      Nombre *
      <input type="text" name="nombre" id="nombre" required
        value="<?= $isEdit ? htmlspecialchars($genre->nombre) : '' ?>">
    </label>

    <label>
      Descripción *
      <textarea name="descripcion" id="descripcion" required><?= $isEdit ? htmlspecialchars($genre->descripcion) : '' ?></textarea>
    </label>

    <label>
      URL de imagen
      <input type="text" name="imagen" id="imagen"
        value="<?= $isEdit ? htmlspecialchars($genre->imagen ?? '') : '' ?>">
    </label>

    <button type="submit"><?= $isEdit ? "Guardar cambios" : "Agregar género" ?></button>
    <a href="admin/generos">Cancelar</a>
  </form>
</main>

<?php
require_once "app/views/templates/layout/footer.phtml";
?>
